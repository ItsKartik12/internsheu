# Base URL of the backend API. Leave unset (or delete this file's copy at
# .env.local) to run the frontend against its local mock data only —
# src/services/api.js falls back gracefully when this isn't reachable.
VITE_API_BASE_URL=http://localhost:5000
